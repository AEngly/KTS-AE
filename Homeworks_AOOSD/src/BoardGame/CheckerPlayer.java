package BoardGame;

public class CheckerPlayer extends Player {

	// Use enum for colour

	public CheckerPlayer(String input) {
		super(input); // Calls player constructor and sets the colour of the player accordingly.
	}
}
